# Book Collection

A mobile book tracking application built with React Native and Expo. Users can add, edit, delete, search, and rate books in their personal collection with support for cover images and notes.

## Setup

Install dependencies and start the development server:
```bash
npm install
npx expo start